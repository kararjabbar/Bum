<?xml version="1.0" encoding="utf-8"?>
<!--
  v1.2: واجهة مخزن المفاتيح كلها من اليمين لليسار (RTL).
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layoutDirection="rtl"
    android:background="?attr/bumBgPrimary">

    <!-- Header — RTL: زر الرجوع يسار (بصرياً على اليمين في RTL) + العنوان على اليمين -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layoutDirection="rtl"
        android:gravity="center_vertical"
        android:padding="20dp"
        android:paddingTop="8dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_arrow_back"
            android:background="@drawable/bg_icon_btn"
            android:tint="?attr/bumTextSecondary"
            android:contentDescription="back" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layoutDirection="rtl"
            android:layout_marginStart="12dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🔑  مخزن المفاتيح"
                android:textColor="?attr/bumTextPrimary"
                android:textSize="18sp"
                android:fontFamily="sans-serif-medium"
                android:textDirection="rtl"
                android:textAlignment="viewStart" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=""
                android:textColor="?attr/bumAmber"
                android:textSize="11sp"
                android:layout_marginTop="0dp"
                android:visibility="gone" />
        </LinearLayout>
    </LinearLayout>

    <EditText
        android:id="@+id/et_search"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:background="@drawable/bg_input_field"
        android:hint="🔍  ابحث: اسم، فئة، ملاحظة..."
        android:textColor="?attr/bumTextPrimary"
        android:textColorHint="?attr/bumTextHint"
        android:textSize="13sp"
        android:inputType="text|textNoSuggestions"
        android:singleLine="true"
        android:imeOptions="actionSearch"
        android:importantForAutofill="no"
        android:textCursorDrawable="@null"
        android:textDirection="rtl"
        android:textAlignment="viewStart"
        android:gravity="start|center_vertical"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="10dp"
        app:layout_constraintTop_toBottomOf="@id/header"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?attr/bumBorder"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/et_search" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_vault"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layoutDirection="rtl"
        android:clipToPadding="false"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="14dp"
        android:paddingBottom="120dp"
        android:scrollbars="none"
        app:layout_constraintTop_toBottomOf="@id/divider"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <LinearLayout
        android:id="@+id/layout_empty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layoutDirection="rtl"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/rv_vault"
        app:layout_constraintBottom_toBottomOf="@id/rv_vault"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔑"
            android:textSize="40sp" />
        <TextView
            android:id="@+id/tv_empty_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="لا مفاتيح محفوظة بعد"
            android:textSize="14sp"
            android:textColor="?attr/bumTextDim"
            android:textDirection="rtl"
            android:textAlignment="center"
            android:gravity="center"
            android:layout_marginTop="10dp" />
    </LinearLayout>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_add_round"
        android:contentDescription="add"
        app:backgroundTint="?attr/bumAmber"
        app:tint="@color/black"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="32dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
